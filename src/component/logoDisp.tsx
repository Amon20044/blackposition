"use client";
import Image from "next/image";
import "./login.css";

export default function LogoDisplay() {
    const scale = typeof window !== "undefined" && window.innerWidth <= 1100 ? 1 : 1.1;

    return (
        <div className="">
            
        </div>
    );
}
